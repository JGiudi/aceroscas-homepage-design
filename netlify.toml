[build]
  command = "npm run build"
  publish = "dist"

# SPA: sirve index.html para rutas de React (/, /v2, etc.) sin 404 al recargar.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
